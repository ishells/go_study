package main

func main() {
	// 算术运算符 + - * / %
	// ++(自增) --(自减) 在go语言中是单独的语句，不能放在等号=的右边

	// 关系运算符
	// == != > >= < <=

	// 逻辑运算符
	// &&(逻辑and运算符)  ||（逻辑or运算符） !（逻辑非运算符）

	// 位运算符
	// &(相与)	|(相或)		^（异或运算）	<<(左移n位就是乘以2的n次方)		>>(右移n位就是除以2的n次方)

	// 赋值运算符
	// =	+=		-=		*=		/=		%=		<<=		>>=		&=		|=		^=
}
